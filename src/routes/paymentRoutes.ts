import { Router } from 'express';
import { PrismaClient } from '@prisma/client';

const router = Router();
const prisma = new PrismaClient();


//create a payment intent 

//create a new payment with a specific order



export default router;